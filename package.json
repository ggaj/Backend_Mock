{
  "name": "backend_mock",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "restaurant": "json-server --watch restaurant.json",
    "marketplace": "pm2 start --name marketplace json-server --watch marketplace.json"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
